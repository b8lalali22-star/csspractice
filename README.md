internal : its easiest way but its complex for editing and when we back to styles 
inline : its easiest way we but it in the same line for text for example
external : the logest way but the better one and easy for editing and modife

the error : When I tried to put an error in the code, it ignored the command I gave it and continued running the rest of the code as it is without any changes. When I went to Inspect, it showed the command but with a strikethrough and a warning icon next to it."
